<?php

/**
 * @file
 * Contains profile_module.module.
 */

use Drupal\Core\Routing\RouteMatchInterface;

/**
 * Implements hook_help().
 */
function profile_module_help($route_name, RouteMatchInterface $route_match) {
  switch ($route_name) {
    // Main module help for the profile_module module.
    case 'help.page.profile_module':
      $output = '';
      $output .= '<h3>' . t('About') . '</h3>';
      $output .= '<p>' . t('Profile Module') . '</p>';
      return $output;

    default:
  }
}

/**
 * Implements hook_theme().
 */
function profile_module_theme() {
  return [
    'profile_module' => [
      'render element' => 'children',
    ],
  ];
}
